import { MapperContainer, BookMapper } from '../../ui';
import { Book } from '../../entities';
import { BookUI } from '../../ui';
export declare const mapperContainer: MapperContainer<Book, BookUI, BookMapper>;

import { MapperContainer, QueryMapper } from '../../ui';
import { Query } from '../../entities';
import { QueryUI } from '../../ui';
export declare const mapperContainer: MapperContainer<Query, QueryUI, QueryMapper>;
